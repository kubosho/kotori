language: node_js

node_js:
  - "0.12.7"
  - "3.3.1"
  - "4.0.0"
  - "4.1.1"

sudo: false

cache:
  directories:
    - node_modules

before_script:
  - npm run ci

after_script:
  - $(npm bin)/coveralls < ./coverage/lcov.info
